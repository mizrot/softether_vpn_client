<?xml version="1.0" encoding="utf-8"?>
<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/light_blue_200"/>
    <!-- Rounded corners (adjust radius as needed) -->
    <corners android:radius="100dp" /> <!-- 16dp = soft rounded corners; higher = more rounded -->
    <!-- Optional: Add a border -->
    <stroke
        android:width="2dp"
        android:color="@color/light_blue_200"/> <!-- Optional border color -->

    <!-- Optional: Add padding inside the button -->
    <padding
        android:left="16dp"
        android:top="8dp"
        android:right="16dp"
        android:bottom="8dp" />
</shape>
